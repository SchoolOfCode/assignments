//select the table element
const tableData = "";

const showData = () => {
  //clear existing data
  //get data from codewars api and map over the array to add items to the dom
};

const hideData = () => {
  //clear existing data
};

// HELPER FUNCTIONS
const sortUsersByRank = (a, b) => {
  // sort users by their rank and honor
};

const getUserData = () => {
  // fetch to the server to get the code wars info
};

const showUserData = ({ user, index }) => {
  // make a table row
  // append the table data to it
  // append row to the table
};

const makeTableDataItem = item => {
  // create table data and add its contents
};

const displayRank = username => {
  // find how to get the images from code wars
  // HINT: look at your profile
  // put image on the page
};
